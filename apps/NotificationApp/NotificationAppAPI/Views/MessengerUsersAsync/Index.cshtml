@model IEnumerable<MessageLibrary.MessengerUserGroup>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MessengerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MessengerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MessengerUserGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MessengerUserGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MessengerUserGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
